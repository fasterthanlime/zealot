import { IStore } from "../types/index";
import * as Msgs from "../types/msgs";
import { Watcher } from "./watcher";
import ps from "./persistent-state";
import * as actions from "../actions";

export default function(watcher: Watcher) {
  // muffin
}
